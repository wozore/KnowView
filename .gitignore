# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
/build/
/dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# 测试 / 类型检查缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
htmlcov/
.hypothesis/

# Jupyter
.ipynb_checkpoints/

# ===== 操作系统生成文件 =====
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# ===== 编辑器 / IDE =====
.idea/
.vscode/
*.swp
*.swo
*~

# ===== 人工维护内容（data/manual） =====
# 热点管线 v2 需人工修改的内容（字幕清单 / 关键词提纯候选 / 待补工具卡等）
# 集中放在 data/manual/，固定格式生成。约定：入库（维护者可见/可提交），
# 但 build-dist.js 不复制到 dist/（不发布到站点）。此目录无需忽略规则——
# 空目录 git 不追踪，有文件即入库；留此注释说明意图，防误加忽略。
# ===== 目录生成器临时文件 =====
data/manual/tools/catalog-drafts/
config/catalog-generator.local.json
config/browser.local.json
data/catalog/.catalog.lock
data/catalog/.transactions/
data/catalog/.staging/
data/catalog/.backup/
# 系统级契约文档与手册白名单入库；临时计划与本地工作稿不入库
/docs/*
!/docs/architecture.md
!/docs/content-quality.md
!/docs/decisions.md
!/docs/hotspot-workflow.md
!/docs/operations.md
!/docs/requirements.md
!/docs/manual/
/docs/manual/**
!/docs/manual/**/*.md

# ===== 第三方 Skills 插件缓存 =====
# Claude 第三方技能缓存不入库；各工具的项目配置在各自目录独立维护
.claude/skills/

# ===== Claude Code / AI 工具会话数据 =====
# 会话元数据频繁变化，不入库
.claudian/

# ===== Claude Code 临时 worktree =====
# Claude Code 创建的嵌套 worktree 是独立 Git 工作区，不应作为主仓库 gitlink 提交
.claude/worktrees/

# ===== Claude Code 本地权限/会话配置 =====
# 本地权限规则与个人环境配置，不入库
.claude/settings.json
.claude/settings.local.json

# ===== Obsidian =====
# .obsidian 为符号链接，指向外部笔记库（MyNote/AI归档知识库），非工程交付物；
# 整个目录忽略（含符号链接文件形态与目录形态），取消历史误跟踪的配置（app.json 等）
.obsidian
.obsidian/

# ===== 脱离版本控制的本地工作稿 =====
# 开发计划与反馈表截图只保留在维护者本机（个人开发记录），不作为公开交付物；
# 开发日志已入库于 docs/manual/dev-log.md（开发过程记录，公开可见）
/开发计划.md
/用户反馈收集表.png
# 第三方技能安装器锁文件（.claude/skills/ 已不入库，锁文件随本地状态变化）
/skills-lock.json

# ===== 脚本生成的临时输出 =====
# 分析脚本运行产生的中间/输出文件（若生成到这些位置）
*.tmp
*.bak
/output/
/outputs/

# ===== 敏感信息（API Key / 环境变量） =====
.env
.env.*
!.env.example
*.secret
*_secret.*
credentials.json
service-account.json
*.log

# ===== 调查原始数据（含受访者个人信息，不入库） =====
# 已脱敏的编码 CSV、问卷 PDF 和分析脚本在 resources/survey/ 下受版本控制
