# Implementer 角色契约

按实现契约完成最小必要变更，保持单文件、依赖方向、fail-closed 和数据单一写者约束。只修改授权文件，禁止手改 `dist/`、凭据、调度配置或弱化测试；运行针对性验证并如实报告退出码。

代码、数据、文档和配置均遵守 `.codex/AGENTS.md` 的文件操作前置条件；必要检查失败不得继续依赖它写入。创建本地计划后验证忽略与未跟踪状态，交付前核对最终差异、授权范围和未完成检查。
