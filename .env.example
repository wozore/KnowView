# ===== 知览 KnowView 本地环境变量模板 =====
# 用法：复制本文件为 .env 并填入真实值。
# .env 已被 .gitignore 忽略，不会进版本库；本模板只含占位符，可安全提交。
# 项目约定：所有密钥只经环境变量注入（进程 env 或 .env），绝不写入代码/配置。
#
# ── 智谱 ZhipuAI（当前默认外部 provider）─────────────────────
# 接入智谱 Anthropic Messages 兼容端点，匹配 Lite 套餐 / 编程计划；默认模型 glm-5.3-flash。
# 同一 Key 也可供目录/新闻显式选择的 zhipu_web_search 使用；搜索按智谱工具调用计费。
ZHIPU_API_KEY=

# 可选：按任务覆盖外部调用模型（缺省 glm-5.3-flash；如需可换 glm-4.5-air / glm-4-flash 等）
# KNOWVIEW_CLASSIFY_MODEL=glm-5.3-flash
# KNOWVIEW_SUMMARIZE_MODEL=glm-5.3-flash
# KNOWVIEW_REVIEW_MODEL=glm-5.3-flash
# KNOWVIEW_LOCALIZE_MODEL=glm-5.3-flash

# ── DeepSeek（历史 provider，可切回）─────────────────────────
# 配置后可将 provider 开关切回 deepseek（registry / config 覆盖）；缺省/失败自动
# 回退 L0 规则式，构建永不因 LLM 阻塞（B16 成本/可靠性约定）。
DEEPSEEK_API_KEY=

# ── Tavily 检索（目录生成器官方来源发现/正文抓取）──────────────
# Search/Extract 默认走 keyless（X-Tavily-Access-Mode: keyless），免费、不耗账号积分，
# 可不配置 TAVILY_API_KEY。key 仅用于 keyless 小时额度耗尽时的自动回退，以及
# map/crawl/research 等 keyed 端点（当前目录生成器未使用）。
TAVILY_API_KEY=

# 可选：强制认证模式（auto|keyless|keyed，默认 auto）。keyless 被封 IP 或异常时可
# 一键切回全部 Bearer，不改代码。
# TAVILY_ACCESS_MODE=auto

# 可选：显式指定分类 provider（不设时 L1 分类不启用，走 L0 规则式）
# 新命名 KNOWVIEW_* 优先，兼容旧名 INFOCATCHER_CLASSIFY_PROVIDER。
# KNOWVIEW_CLASSIFY_PROVIDER=zhipu

# ── 平台采集 key（可选；CI 中也可用 GitHub Secrets 注入同名变量）──
# YOUTUBE_API_KEY=
# X_API_KEY=
